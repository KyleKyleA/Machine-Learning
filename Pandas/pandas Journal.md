# Journal for Pandas


# Thoughts

Pandas is just learning about how to create and structure tables using different panda functions similar to numpy's but the whole goal of it was to create a table to 

execute onto the terminal and learning how to plot the values onto the graph for more of a visualization to see compared to what you see onto the terminal.

lots of stuff learned today what I've practiced a bit yesterday such as reading and writing from a csv file which is easier to get by downloading the entire data set and

using it to manipulate data by either filling it in. Or otherwise selecting and retrieving information to insight to see what going in the dataset just like how in SQL.

built a mini project using the information I'd insighted and a bit of ai assistance for some things I don't understand but after learning I know how to plot the graph

whether the value is in numerical value or alphabetical which seems very useful when handling whole sets of data.

# Project idea's

These's dataset have been good idea's for a simple machine learning project i could use to put on my resume 
-> focuses on real world problems and issues that people may be facing this could be just to predict oncoming new data for companies to look at to prevent the problem
-> could also focus on sports since that's what I like based on the statistics and look to make predicts on the new set of scoring or new score record
-> one more major concept to go through is sci kit learn since it's going to be the tool to help use predict models and using numpy's and pandas to create numbers and tables to eventually create our project


